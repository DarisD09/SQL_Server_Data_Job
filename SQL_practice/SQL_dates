SELECT 
    COUNT (job_id) AS job_id_count,
    EXTRACT(MONTH from Job_posted_date) as MONTH
FROM 
    job_postings_fact
WHERE 
    job_title_short = 'Data Analyst'
GROUP BY 
    MONTH
ORDER BY
    job_id_count DESC
